# deploy/k8s/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: process-rec-api
  namespace: matterize
  labels:
    app: process-rec-api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: process-rec-api
